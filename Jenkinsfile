@Library('jenkins-helpers') _
pipeline {

    agent any

    stages {
        stage('Build') {
            steps {
                echo "Build"
            }
        }

        stage('Deploy') {
            steps {
                echo "Dpleo"
            }
        }
    }
}
