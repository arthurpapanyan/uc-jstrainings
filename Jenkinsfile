@Library('jenkins-helpers') _

pipeline {

    agent any

    stages {
        stage('Build') {
            steps {
                echo "Build"
                script{
                Builders('Starting Jojo')

                }

            }
        }

        stage('Deploy') {
            steps {
                echo "Dpleo"
            }
        }
    }
}
