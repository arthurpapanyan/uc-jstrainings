@Library('jenkins-helpers') _
pipeline {

    agent any

    stages {
        stage('Build') {
            steps {
script{
}                meth("act")

            }
            echo "done"
        }

        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}
